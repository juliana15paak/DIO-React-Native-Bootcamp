import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputer: {
    backgroundColor: '#e5bf3c',
    width: '100%',
    color: 'black',
    fontSize: 20,
    height: 40,
    borderRadius: 10,
    padding: 8,
    textAlign: 'center'
  }
});